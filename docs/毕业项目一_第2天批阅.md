# 📝 毕业项目一 · 第2天 批阅细则

> 项目：GitHub 趋势仓库分析平台（多人协同）
> 阶段：第2天 — analyzer 分析层
> 关联仓库：`D:\Project_one`（src/analyzer.py 已落地）
> 总分：**95/100** 🟢（拓展 +10）| 判定：**通过 ✅**

---

## 一、名词解释（每题5分，共20分）→ **19/20**

### 第1题
**题目**：为什么分析层要用 pandas 而不是纯 Python 循环？

**你的作答**（原文）：
> pandas专门用来处理结构化数据，天然适合用于该项目，而纯python循环性能开销大，不利于处理大量数据

**评分**：5/5 ✅
**建议**：可补充一句：pandas 底层用 C 实现（numpy），向量化运算比 Python 循环快几十到几百倍。
**比较**：核心两点（结构化+性能）全对，一致。

### 第2题
**题目**：`logging` 和 `print` 调试的区别

**你的作答**（原文）：
> logging是python专门用于记录日志的库，其可输出到控制台和文件等，输出内容包含详细的时间、行号、报错内容等重要信息，而print需要手动在代码里插入，后续还需要删除，因此print只适合用于小项目的初步判断调试，大项目还是需要使用logging

**评分**：5/5 ✅
**建议**：无。回答完整（输出目标、时间/行号/级别、可配置、print 的缺点）。
**比较**：一致且超出预期。

### 第3题
**题目**：pandas 中 `df.groupby("列")["值"].mean()` 的链式含义

**你的作答**（原文）：
> 对列进行分组并且对同一组的值求平均

**评分**：4/5 🟡（-1：省略了中间环节）
**建议**：更精确的拆解：① `df.groupby("列")` → 按某列的值分组；② `["值"]` → 从每组里选一列；③ `.mean()` → 对每组该列求平均。你的回答少了第②步"选中某列"。
**比较**：意思对但链条解释不完整——这是概念题，链式的每一环都值得点名。

### 第4题
**题目**：类型提示 `def analyze(items: list[dict]) -> dict[str, float]` 表达什么？

**你的作答**（原文）：
> 表示函数analyze的参数items是列表嵌套字典，函数返回的值是字典，字典的键是字符串值是浮点数

**评分**：5/5 ✅
**建议**：无，完整准确。
**比较**：一致。

---

## 二、代码阅读（每题10分，共20分）→ **18/20**

### 第1题
**题目**：groupby 聚合代码，输出结果？

**你的作答**（原文）：
```
language
Python	3	126
Jupyter	1	50
```

**评分**：8/10 🟡（-2：mean 精度）
**建议**：count 正确（Python 3、Jupyter 1）；但 `mean` 实际输出是 **126.666667**（100+200+80=380，380/3=126.66...），不是 126。DataFrame 默认会保留小数。
**比较**：结构理解正确，四舍五入导致精度丢失——写代码时让机器算，别手算取整。

### 第2题
**题目**：Counter 代码，输出结果？

**你的作答**（原文）：
> 输出：('2024',3) / 3

**评分**：10/10 ✅
**建议**：无，完全正确（most_common(1)[0] 返回裸元组；len(c) 统计不同年份数）。
**比较**：一致。

---

## 三、代码补全（10分）→ **10/10**

**题目**：补全 summarize_languages 的三个空位（Counter/len/most_common）

**你的作答**（原文）：
```python
counter = Counter(languages)
logger.info(f"语言统计完成，共{len(counter)}种语言")
return counter.most_common()
```

**评分**：10/10 ✅
**建议**：无。
**比较**：一致。

---

## 四、手写代码（30分）→ **28/30**

**题目**：写 analyze_repos（4点要求见考题）

**你的作答**（原文）：
```python
import pandas as pd
import logging
from collections import Counter
from src.fetcher import fetch_top_repos

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    filename="log.log",
    filemode="w"
)

def analyze_repos(items: list[dict])->dict:
    logging.info("analyze_repo开始")

    if not items:
        logging.warning("items参数为空")
        return {"avg_stars": 0.0, "max_stars_name": '', "top_language": ''}

    rows = []
    for item in items:
        rows.append({
            "name": item["name"], "stars": item["stars"], "forks": item["forks"],
            "language": item["language"] or "N/A", "created_at": item["created_at"]
        })

    languages = [r["language"] for r in rows]
    count_languages = Counter(languages)

    df = pd.DataFrame(rows)
    avg_star = round(df["stars"].mean(), 1)
    max_stars_name = max(rows, key=lambda x: x["stars"])["name"]
    top_language = count_languages.most_common(1)[0][0]

    analyze_dict = {"avg_stars": avg_star, "max_stars_name": max_stars_name, "top_language": top_language}
    logging.info("analyze_repo完成")
    return analyze_dict
```

**评分**：28/30 🟡（-2）

**建议**：
| 题目要求 | 状态 |
|---------|:--:|
| pandas DataFrame（name/stars/forks/language/created_at） | ✅ |
| avg_stars（round 1位）+ max_stars_name + top_language | ✅ |
| logging 记录开始/完成 | ✅ |
| items 为空返回空统计 + warning | ✅ |

**2处改进（不影响通过）**：
1. **死 import**：`from src.fetcher import fetch_top_repos` 导入了但没调用——代码审查会拦这个。
2. **logging 配置位置**：`basicConfig(filename="log.log")` 写在模块顶层且路径写死。更规范是放入口/配置模块，或用相对路径基于配置。另外既然已有 df，`max_stars_name` 用 pandas 更顺：`df.nlargest(1, "stars")["name"].iloc[0]`。

**比较**：功能完整、空值处理到位、logging 用法正确，扣分仅在代码整洁度——正是 D4 代码审查演练会讨论的点。

---

## 五、设计题（20分）→ **20/20**

**题目**：依赖倒置 + 接口设计（2问）

**你的作答**（原文）：
> fetcher只负责获取数据不需要额外依赖用不上的pandas接口，这样符合接口隔离原则

> 只要analyze的接口返回的是足够丰富的结构化分析数据，而不是为某个东西定制的产物，后续的visualizer层就不需要修改analyze

**评分**：10/10 + 10/10 = 20/20 ✅
**建议**：无。第2问尤其好——"返回通用丰富结构而非定制产物"，这正是接口稳定的核心。
**比较**：一致且深刻，SOLID-D 理解到位。

---

## 📌 拓展内容（+10）→ **10/10**

**题目**：`df.nlargest(3, "stars")` 和 `df.sort_values(...).head(3)` 的区别（提示：NaN 处理）

**你的作答**（原文）：
> 二者的作用相同，但在处理NaN时，前者会自动过虑掉NaN而后者不能

**评分**：10/10 ✅
**建议**：无。完全正确——nlargest 内部丢弃 NaN，sort_values 会把 NaN 排在末尾，head(3) 可能包含 NaN 行。
**比较**：一致。

---

## 🧪 实际代码审查（D:\Project_one\src）

你的答案已落地为真实文件，审查如下：

### fetcher.py ✅（含2条改进建议）
- ✅ 函数名/参数/Authorization 格式/per_page/异常处理全部正确
- 🟡 建议1：`GITHUB_TOKEN` 未设置时 `f"token {None}"` 静默失败，应加空值检查：
  ```python
  if not GITHUB_TOKEN:
      raise ApiError(0, "缺少 GITHUB_TOKEN，请检查 .env")
  ```
- 🟡 建议2：顶部留了被注释掉的 ApiError 类定义（死代码），建议删除保持整洁

### analyzer.py ✅（含2条改进建议）
- ✅ DataFrame/统计/logging/空值处理全部正确
- 🟡 建议1：`from src.fetcher import fetch_top_repos` 未使用，删掉
- 🟡 建议2：`filename="log.log"` 写死，可移到配置

### exceptions.py ✅ 干净正确（status_code + message + super().__init__）
### __init__.py ✅ 空包标记正确

> 安全性核查：✅ token 仅环境变量读取、无硬编码、无密钥打印。

---

## 📊 总分汇总

| 部分 | 满分 | 得分 |
|------|:--:|:--:|
| 一、名词解释 | 20 | 19 |
| 二、代码阅读 | 20 | 18 |
| 三、代码补全 | 10 | 10 |
| 四、手写代码 | 30 | 28 |
| 五、设计题 | 20 | 20 |
| 拓展（nlargest） | +10 | +10 |
| **总分** | **100** | **95 + 拓展10** |

## 🎯 判定：**通过 ✅** — 进入项目一第3天（visualizer 可视化层）
