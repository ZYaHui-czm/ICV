# 📝 Python 45天每日复盘考核 — 第1周

> 学习范围：Lv.1 — 基础感知（第1~7天）
> 在 VSCode 中 **Ctrl + 点击** 下方链接可快速跳转。
# 本文档参考了["Python-100-Days"](https://github.com/jackfrued/Python-100-Days)这个仓库的内容。感谢作者的开源精神

---

## 📑 目录（Ctrl+点击跳转）

| <a href="#day1">第1天</a> · <a href="#day2">第2天</a> · <a href="#day3">第3天</a> · <a href="#day4">第4天</a> · <a href="#day5">第5天</a> · <a href="#day6">第6天</a> · <a href="#day7">第7天(复盘)</a> |

---

| 后续周次 | 文档 |
|---------|------|
| **第2周** | `第2周_每日复盘考核.md` |
| **第3周** | `第3周_每日复盘考核.md` |
| **第4周** | `第4周_每日复盘考核.md` |
| **第5周** | `第5周_每日复盘考核.md` |
| **第6周** | `第6周_每日复盘考核.md` |
| **第7周** | `第7周_每日复盘考核.md` |

---

> ⚡ **说明**：每日格式为「📌 题目 + 作答 → 批改/打分 → 错误总结」

---

# 🟢 第1周：Lv.1 基础感知

---

## 🗓️ 第1天 <a id="day1"></a>

**主题**：变量、数据类型、print/input

**得分**：78/100 🟡

---

### 一、名词解释（每题5分，共20分）
1. **变量**     //作答：变量就是用来储存一个东西，以便后续程序调用 ✅
2. **f-string**     //作答：f-string是print输出里面的格式化方法，常见的还有"%d" 🟡 漏了关键：f-string用`f"..."`语法，花括号`{}`放变量
3. **类型转换**（如 `int()`、`str()`）     //作答：类型转换是把一个类型转换成另一个类型，比如int(input())是把获取输入的字符串类型转换为整数类型 ✅
4. **`input()` 的返回值类型**     //作答：input()返回字符串类型 ✅

### 二、代码阅读（每题10分，共20分）
```python
a = "10"
b = "20"
print(a + b)   // 作答：10 + 20 ❌ 正确答案：1020（字符串拼接，不是加法）
```
```python
x = 5
y = str(x)
z = "10"
print(y + z)   // 作答：5 + 10 ❌ 正确答案：510（"5"+"10"拼接）
```

### 三、代码补全（10分）
```python
a = int(input("第一个数："))
b = int(input("第二个数："))     // ✅
print(f"和：{a+b}")              // ✅
print(f"积：{a*b}")              // ✅
```

### 四、手写代码（30分）
**题目**：问姓名 → 问出生年份 → 计算年龄 → 输出"XXX，你今年XX岁"
```python
name = input("你叫什么：")
age = int(input("你什么时候出生的："))
age_num = 2025 - age
print(f'{name},你今年{age_num}岁')
```
✅ 功能完全正确！30/30

### 五、概念辨析（20分）
| 概念A | 概念B | 作答与点评 |
|-------|-------|-----------|
| `print(3 + 5)` | `print("3" + "5")` | //作答：一个在进行数学运算并输出结果，一个是输出那一句话 ✅ 核心理解正确 |
| `int` 类型 | `str` 类型 | //作答：int类型能够参与数学运算，str类型不能进行数学运算 ✅ 补充：str也可用`+`(拼接)和`*`(重复)，但不是数学运算 |

---

### ❌ 错误总结

| 错误答案 | 正确答案 | 原因 |
|---------|---------|------|
| `"10"+"20"` = `10+20` | `"1020"` | 字符串有引号，`+`是拼接不是加法 |
| `str(5)+"10"` = `5+10` | `"510"` | 同上，`str(5)`变成文本"5" |

### 🔍 理解缺口
- 看到 `"10"` 心里当数字处理了 → 判断标准：**有没有引号**

### 📌 核心教训
> **引号决定类型：`"10"`是文本不是数字，`+`是拼接不是加法**

---

## 🗓️ 第2天 <a id="day2"></a>

**主题**：字符串操作、索引、切片

**得分**：70/100 🟡

---

### 一、名词解释（每题5分，共20分）
1. **字符串索引**（如 `s[0]`）     //作答：s[0]表示索引字符串的第一个数据 ✅
2. **字符串切片**（如 `s[1:4]`）     //作答：从字符串s的第二个数据开始切片，到第五个数据结束 🟡 不准确——切片**左闭右开**，`s[1:4]`取索引1,2,3（第2,3,4个字符），不包含索引4（第5个）
3. **f-string 与普通字符串拼接的区别**     //作答：不知道 🔴 f-string：`f"你好{name}"`，拼接：`"你好"+name`。f-string更简洁高效，自动处理类型转换
4. **`len()` 函数**     //作答：len函数是用来求一个变量的长度 ✅ 精确说：返回序列（字符串/列表等）的元素个数

### 二、代码阅读（每题10分，共20分）
```python
s = "Python"
print(s[0])     # 作答：P ✅
print(s[-1])    # 作答：n ✅
print(s[1:4])   # 作答：ytho ❌ 正确答案：yth（s[1]="y", s[2]="t", s[3]="h"，索引4="o"不被包含）
```
```python
s = "hello world"
print(s.upper())              # 作答：HELLO WORLD ✅
print(s.replace("l", "x"))    # 作答：hexxo world ✅
print(s.count("o"))           # 作答：2 ✅
```

### 三、代码补全（10分）
```python
s = "hello"
result = s[::-1]     // ✅ 完全正确！
```

### 四、手写代码（30分）
**题目**：输入一句话，输出长度、全大写、空格替换为`-`
```python
s = input()
Cap_s = s.upper()
print(Cap_s)
print(s.replace(" ", "-"))
```
🟡 功能部分正确，**漏了第1问**（输出长度），应加 `print(len(s))`。少1个功能扣10分。变量名 `Cap_s` 建议改成小写 `cap_s` 或 `upper_s`

### 五、概念辨析（20分）— 未作答 🔴
| `s[0:3]` | `s[0:3:1]` | //作答：未作答 ❌ 两者等效，`[0:3]`默认步长1，`[0:3:1]`显式步长1 |
| `s.find("a")` | `s.index("a")` | //作答：未作答 ❌ find找不到返回-1，index找不到抛ValueError异常 |

---

### ❌ 错误总结

| 错误答案 | 正确答案 | 原因 |
|---------|---------|------|
| `s[1:4]` = 第2到第5个 | 第2到第4个（索引1,2,3） | **左闭右开**：`[始:终]`不包含终点 |
| `s[1:4]` = `ytho` | `yth` | 切片不含终点索引4的"o" |
| 手写代码漏了 `len(s)` | 需加 `print(len(s))` | 漏看题目要求第1点 |
| 概念辨析未作答 | — | 20分直接丢了 |

### 🔍 理解缺口
- **切片边界规则**：`s[i:j]` 取 i 到 j-1，不包含 j
- **`[::-1]` 用法**：虽然答对了第三题，但需理解——`[始:终:步长]`，步长为-1时从右往左走
- **find vs index**：一个返回-1，一个抛异常

### 📌 核心教训
> **切片左闭右开，`s[i:j]`到j前为止；`s[::-1]`反转字符串，三步法记心头**

---

## 🗓️ 第3天 <a id="day3"></a>

**主题**：列表 + 循环

**得分**：75/100 🟡

---

### 一、名词解释（每题5分，共20分）
1. **列表（list）**     //作答：列表是用来存储元素的集合 ✅
2. **`list.append(x)` 和 `list.extend([...])` 的区别**     //作答：`.append()`是将元素整体添加进列表里，而`.extend()`是将元素拆分之后再分别添加进列表里 ✅ 精准！
3. **`for` 循环**     //作答：for循环是将对象里的每一个元素进行遍历 ✅
4. **`range(5)`、`range(2,5)`、`range(1,6,2)` 的区别**     //作答：range(5)从0到4，range(2,5)从2到4，range(1,6,2)从1到5步长为2 ✅ 完全正确

### 二、代码阅读（每题10分，共20分）
```python
nums = [1, 2, 3]
nums.append(4)
nums.pop()
nums.append(5)
print(nums)     # 作答：[1,2,3,5] ✅
```
```python
squares = []
for i in range(1, 5):
    squares.append(i ** 2)
print(squares)  # 作答：[1,4,9,16] ✅
```

### 三、代码补全（10分）
```python
nums = [3, 1, 4, 1, 5, 9, 2, 6]
nums = nums.sort(reverse = True)     // ❌ 两个错误：1) sort()原地排序返回None，赋值后nums变None；2) reverse=True是从大到小，题目要求从小到大
# ✅ 正确：nums.sort()
```

### 四、手写代码（30分）
**题目**：用for循环+if生成1~10中所有偶数的列表，输出列表及其长度
```python
double_num = [x for x in range(11) if x % 2 == 0]
print(double_num)
print(len(double_num))
```
🟡 功能正确但两个小问题：1) 题目要求for+if循环而非推导式；2) range(11)含0，题目要求1~10
# ✅ 正确写法：evens = []; for x in range(1,11): if x%2==0: evens.append(x)

### 五、概念辨析（20分）
| 概念A | 概念B | 作答与点评 |
|-------|-------|-----------|
| `list` 类型 | `str` 类型 | //作答：list存储元素，str本身就是元素 🟡 漏了关键区别：list可变（可增删改），str不可变 |
| `nums[2]` | `nums[2:3]` | //作答：nums[2]是索引下标为2的值，nums[2:3]是切片从下标2到3 ✅ 补充：nums[2]返回单个元素，nums[2:3]返回列表 |

---

### ❌ 错误总结

| 错误 | 正确 | 原因 |
|------|------|------|
| `nums = nums.sort(reverse=True)` | `nums.sort()` | sort()原地排序返回None；reverse=True是从大到小 |
| 推导式代替for+if | 手写for+if循环 | 看清题目要求 |
| `range(11)`含0 | `range(1,11)` | 1~10的范围 |

### 📌 核心教训
> **sort()原地改，返回None**
```python
nums = [3, 1, 4, 1, 5, 9, 2, 6]

print(nums.sort())  //输出：None,这是一个刻意的防误导设计，Python 创始人 Guido van Rossum：如果一个方法会原地修改对象，就不应该返回修改后的对象。通过返回None来告诉你这个方法是在修改原对象；类似的还有
lst = [1, 2, 3]

print(lst.append(4))    # None
print(lst.reverse())    # None
print(lst.extend([5]))  # None
print(lst.insert(0, 0)) # None
```
### 另外：列表方法里的pop方法删除元素时会得到被删除的元素，默认删除最后一个元素。

---

## 🗓️ 第4天 <a id="day4"></a>

**主题**：条件判断 if/elif/else + 逻辑运算符

**得分**：76/100 🟡

---

### 一、名词解释（每题5分，共20分）
1. **`if`、`elif`、`else` 的执行顺序**     //作答：先执行if然后elif最后else 🟡 描述太简略——正确顺序是：从上到下依次检查，if不成立才检查elif，所有条件都不成立才执行else
2. **`and` 和 `or` 的区别**     //作答：and需要两个条件都为真，or只需要一个为真 ✅
3. **"短路逻辑"**     //作答：and的第一个条件为假时不会考虑第二个条件，or是第一个条件为真时就不再考虑第二个条件 ✅ 完全正确！
4. **Falsy 值（假值）**     //作答：falsy自带假值 🔴 太模糊了！需要具体：`0`、`""`（空字符串）、`[]`（空列表）、`None`、`False` 都是假值

### 二、代码阅读（每题10分，共20分）
```python
x = 10
if x > 5:
    print("A")
if x > 8:         # 注意这是独立if，不是elif！
    print("B")
else:
    print("C")
# 作答：AB ✅ 分析：x>5输出A；x>8为True输出B，所以else不执行
```
```python
a = 0          # Falsy
b = "hello"    # Truthy
c = []         # Falsy
if a and b:      # 0 and "hello" → False（短路）
    print("1")
elif b or c:     # "hello" or [] → True（短路）
    print("2")   # 输出2
else:
    print("3")
# 作答：2 ✅
```

### 三、代码补全（10分）
```python
score = 85
if score >= 90:          # ✅
    print("优秀")
elif score >= 80:        # ✅
    print("良好")
elif score >= 60:        # ✅
    print("及格")
else:                    # ✅
    print("不及格")
```
🟡 逻辑正确，但注意 `elif` 不要前面加空格，格式规范化

### 四、手写代码（30分）
**题目**：判断一个数能否同时被3和5整除
```python
num = int(input())
if num // 3 == 0 and num // 5 == 0:
    print("yes")
else:
    print("no")
```
🔴 **关键错误**：`//` 是**整除**（取商），`%` 才是**取余**（取余数）！
- `15 // 3 = 5`，`5 == 0` 为 False ❌
- `15 % 3 = 0`，`0 == 0` 为 True ✅
✅ 正确写法：`if num % 3 == 0 and num % 5 == 0:`

### 五、概念辨析（20分）
| 概念A | 概念B | 作答与点评 |
|-------|-------|-----------|
| `if x > 5 and x < 10` | `5 < x < 10` | //作答：意义等价 ✅ Python支持链式比较，两者等价 |
| `if age >= 18:` | `if age = 18:` | //作答：一个大于等于，一个等于 🟡 但严重问题没指出——`if age = 18:` 在Python里是**语法错误**！`=`是赋值不能用在条件里，会报 `SyntaxError` |

---

### ❌ 错误总结

| 错误 | 正确 | 原因 |
|------|------|------|
| `num // 3 == 0` | `num % 3 == 0` | `//`是整除取商，`%`才是取余！**15//3=5≠0**，但**15%3=0** |
| "falsy自带假值" | 0/" "/[]/None/False | 需要具体说出哪些是假值 |
| `if age = 18:` 能执行 | 会报语法错误 | `=`是赋值，不能当条件判断 |

### 🔍 理解缺口
- `//` 和 `%` 混淆——**`//` 整除**（结果是商），**`%` 取余**（结果是余数）

### 📌 核心教训
> **判断整除用 `%` 不用 `//`；等号比较 `==` 赋值单 `=`；**
> **falsy 记清楚：0空None False**

---

## 🗓️ 第5天 <a id="day5"></a>

**主题**：函数定义 + return + 参数

**得分**：78/100 🟡

---

### 一、名词解释（每题5分，共20分）
1. **函数**     //作答：函数就是一个封装好的工具箱 ✅
2. **`return` 和 `print` 的区别**     //作答：return会保留结果而print只会输出结果不保留结果 ✅
3. **默认参数**     //作答：默认参数指函数定义时设置的，当调用函数时没带参数会使用默认参数，默认参数不应该使用可变数据类型！✅ 还指出了可变数据类型的坑，优秀！
4. **`*args` 和 `**kwargs`**     //作答：*args可接收多个位置参数，**kwargs可接收多个关键字参数 ✅

### 二、代码阅读（每题10分，共20分）
```python
def calc(x, y):
    return x * y
    print("完成")   # return后不执行
result = calc(3, 4)
print(result)        # 作答：12 ✅
```
```python
def add_item(item, lst=None):
    if lst is None:
        lst = []
    lst.append(item)
    return lst
print(add_item(1))      # 作答：1 🟡 输出是[1]（列表形式），不是1
print(add_item(2))      # 作答：2 🟡 输出是[2]，不是2
```

### 三、代码补全（10分）
```python
def is_even(n):
    if n % 2 == 0:
        return yes     # ❌ yes没加引号，会被当作变量名→NameError
    else:
        return no      # ❌ 同上
```
🔴 两个语法错误：`yes` 和 `no` 没加引号，Python会认为它们是变量名。
✅ 正确写法：`return True` / `return False`（布尔值）或 `return "yes"` / `return "no"`（字符串）

### 四、手写代码（30分）
**题目**：函数greet(name, greeting="你好")，返回问候字符串，调用两次
```python
def greet(name, greeting='你好'):
    return greeting + name
    print(greet(xiaoming)    # ❌ return后代码不执行；xiaoming缺引号
    print(xiaoming, hello)   # ❌ 语法错误
```
🔴 问题较多：1) return后不会执行后面的print；2) `xiaoming` 没加引号；3) 调用语法错误；4) `greeting + name` 没有空格
🟡 核心思路（定义函数、返回字符串）是对的
✅ 正确写法：
```python
def greet(name, greeting="你好"):
    return f"{greeting}，{name}！"
print(greet("小明"))          # 你好，小明！
print(greet("小红", "早上好")) # 早上好，小红！
```

### 五、概念辨析（20分）
| 概念A | 概念B | 作答与点评 |
|-------|-------|-----------|
| 位置参数 | 关键字参数 | //作答：位置参数只有符合顺序才能得到正确返回值，关键字参数不做顺序要求 ✅ |
| 函数定义 `def f():` | 函数调用 `f()` | //作答：定义用def+函数名+参数，调用只需函数名 ✅ |

---

### ❌ 错误总结

| 错误 | 正确 | 原因 |
|------|------|------|
| `return yes` | `return True` 或 `return "yes"` | `yes` 不加引号被当成变量→NameError |
| `greet(xiaoming)` | `greet("小明")` | 变量名要加引号才是字符串 |
| return后还写代码 | return后代码永不执行 | return = 函数结束 |

### 📌 核心教训
> **字符串要加引号，变量名不用加；return打完就收工，后面代码全白写**


###另：在参数列表中用/设置强制位置参数（positional-only arguments），用*设置命名关键字参数。所谓强制位置参数，就是调用函数时只能按照参数位置来接收参数值的参数；而命名关键字参数只能通过“参数名=参数值”的方式来传递和接收参数
```python
# /前面的参数是强制位置参数
def make_judgement(a, b, c, /):
    """判断三条边的长度能否构成三角形"""
    return a + b > c and b + c > a and a + c > b


# 下面的代码会产生TypeError错误，错误信息提示“强制位置参数是不允许给出参数名的”
# TypeError: make_judgement() got some positional-only arguments passed as keyword arguments
# print(make_judgement(b=2, c=3, a=1))
```
### Python 中允许函数的参数拥有默认值，但是带默认值的参数必须放在不带默认值的参数之后，否则将产生SyntaxError错误，错误消息是：non-default argument follows default argument，翻译成中文的意思是“没有默认值的参数放在了带默认值的参数后面”。
### 可变参数
Python 语言中可以通过星号表达式语法让函数支持可变参数。
```
# 用星号表达式来表示args可以接收0个或任意多个参数
# 调用函数时传入的n个参数会组装成一个n元组赋给args
# 如果一个参数都没有传入，那么args会是一个空元组
def add(*args):
    total = 0
    # 对保存可变参数的元组进行循环遍历
    for val in args:
        # 对参数进行了类型检查（数值型的才能求和）
        if type(val) in (int, float):
            total += val
    return total


# 在调用add函数时可以传入0个或任意多个参数
print(add())         # 0
print(add(1))        # 1
print(add(1, 2, 3))  # 6
print(add(1, 2, 'hello', 3.45, 6))  # 12.45
```
如果我们希望通过“参数名=参数值”的形式传入若干个参数，具体有多少个参数也是不确定的，我们还可以给函数添加可变关键字参数，把传入的关键字参数组装到一个字典中，代码如下所示。
```
# 参数列表中的**kwargs可以接收0个或任意多个关键字参数
# 调用函数时传入的关键字参数会组装成一个字典（参数名是字典中的键，参数值是字典中的值）
# 如果一个关键字参数都没有传入，那么kwargs会是一个空字典
def foo(*args, **kwargs):
    print(args)
    print(kwargs)


foo(3, 2.1, True, name='骆昊', age=43, gpa=4.95)
```
标准库中的模块和函数
| 函数    | 说明                                                         |
| ------- | ------------------------------------------------------------ |
| `abs`   | 返回一个数的绝对值，例如：`abs(-1.3)`会返回`1.3`。           |
| `bin`   | 把一个整数转换成以`'0b'`开头的二进制字符串，例如：`bin(123)`会返回`'0b1111011'`。 |
| `chr`   | 将Unicode编码转换成对应的字符，例如：`chr(8364)`会返回`'€'`。 |
| `hex`   | 将一个整数转换成以`'0x'`开头的十六进制字符串，例如：`hex(123)`会返回`'0x7b'`。 |
| `input` | 从输入中读取一行，返回读到的字符串。                         |
| `len`   | 获取字符串、列表等的长度。                                   |
| `max`   | 返回多个参数或一个可迭代对象中的最大值，例如：`max(12, 95, 37)`会返回`95`。 |
| `min`   | 返回多个参数或一个可迭代对象中的最小值，例如：`min(12, 95, 37)`会返回`12`。 |
| `oct`   | 把一个整数转换成以`'0o'`开头的八进制字符串，例如：`oct(123)`会返回`'0o173'`。 |
| `open`  | 打开一个文件并返回文件对象。                                 |
| `ord`   | 将字符转换成对应的Unicode编码，例如：`ord('€')`会返回`8364`。 |
| `pow`   | 求幂运算，例如：`pow(2, 3)`会返回`8`；`pow(2, 0.5)`会返回`1.4142135623730951`。 |
| `print` | 打印输出。                                                   |
| `range` | 构造一个范围序列，例如：`range(100)`会产生`0`到`99`的整数序列。 |
| `round` | 按照指定的精度对数值进行四舍五入，例如：`round(1.23456, 4)`会返回`1.2346`。 |
| `sum`   | 对一个序列中的项从左到右进行求和运算，例如：`sum(range(1, 101))`会返回`5050`。 |
| `type`  | 返回对象的类型，例如：`type(10)`会返回`int`；而` type('hello')`会返回`str`。 |

---

## 🗓️ 第6天 <a id="day6"></a>

**主题**：综合项目 — 简易问答系统 CLI

**得分**：87/100 🟢

---

### 一、名词解释（每题5分，共20分）
1. 为什么用**字典**存每道题？     //作答：字典用键值对的方式将数据组织在一起，符合问答系统的要求，而列表天然不适合用在该项目 ✅
2. `enumerate(questions, 1)` 的作用？     //作答：在遍历可迭代对象时同时获取"索引"和"值"，本质是返回枚举对象，每次产出(index,value)元组，本项目中从1开始计数 ✅ 非常精确
3. `input().strip().upper()` 为什么串三个方法？     //作答：input()获取输入，.strip()去掉前后空格，.upper()转大写以便与答案校验，三个并用才能正确获取输入 ✅
4. 什么是 CLI？     //作答：CLI是命令行界面的缩写 ✅

### 二、代码阅读（每题10分，共20分）
```python
q = {"question": "2+2=?", "options": {"A": "3", "B": "4"}, "answer": "B"}
user = input("答案：").strip().upper()
if user == q["answer"]:
    print("对")
else:
    print("错")
# 用户输入"  b  " → strip()→"b" → upper()→"B" → "B"=="B" → True
# 作答：对 ✅
```
```python
def calc(score, total):
    return score / total * 100
print(calc(3, 4))   # 作答：75 ✅  实际输出75.0
print(calc(0, 5))   # 作答：0  ✅  实际输出0.0
```

### 三、代码补全（10分）
```python
questions = [
    {"question": "Python的作者是？",
     "options": {"A": "Van Rossum", "B": "Gates", "C": "Jobs"},
     "answer": "A"},                                    # ✅
    {"question": "Python中 list 是可变还是不可变？",
     "options": {"A": "可变", "B": "不可变"},
     "answer": "A"},                                    # ✅
]
# ...判断
if user == q['answer']:                                 # ✅
    score += 1
```
🟡 逻辑完全正确

### 四、手写代码（30分）
```python
question1 = {
    'question': 'Python的创始年份是？',
    'options': {"A": "1989", "B": "1991", "C": "2000", "D": "2008"},
    'answer': 'B'
}
question2 = {
    'question': '以下哪个是可变数据类型？',
    'options': {"A": "str", "B": "int", "C": "list", "D": "tuple"},
    'answer': 'C'
}
question3 = {
    'question': 'len("Python") 的结果是？',
    'options': {"A": "5", "B": "6", "C": "7", "D": "8"},
    'answer': 'B'
}

questions = [question1, question2, question3]

def run_QA(questions):
    '''运行cli'''
    scoal = 0           # 拼写：score，但一致使用程序正常运行
    total = len(questions)
    for i, q in enumerate(questions, 1):
        print(f'No.{i}question:{q["question"]}')
        for key, value in q['options'].items():
            print(f'{key}.{value}')

        '''获取输入'''
        user_answer = input('please answer:').strip().upper()

        '''判断是否为无效输入'''
        while user_answer not in q['options']:   # 👏 自己加了输入校验！
            user_answer = input('Error!please answer again:').strip().upper()

        if user_answer == q['answer']:
            print('right!')
            scoal += 1
        else:
            print(f'error!The correct answer is {q["answer"]}')
    return scoal, total

'''调用函数'''
if __name__ == '__main__':                     # 👏 竟然用上了Python惯用法！
    correct, total = run_QA(questions)
    print(f'You got {correct} questions right')
    print(f'Accuracy rate {correct / total * 100:.1f}%')
```
🟢 功能完整！超出预期：自带了输入校验 + `if __name__` 惯用法 + 格式化百分比！
🟡 小瑕疵：`scoal` 拼写不规范（应是 `score`）；f-string 中嵌套同类型引号

### 五、概念辨析（20分）
| 概念A | 概念B | 作答与点评 |
|-------|-------|-----------|
| `q["answer"]` | `q.get("answer")` | //作答：不知道 🔴 `q["answer"]`：key不存在时报KeyError；`q.get("answer")`：key不存在返回None |
| `.strip()` | `.upper()` | //作答：.strip()去掉输入的空格，.upper()将输入转为大写 ✅ |

---

### ❌ 错误总结

| 错误 | 正确/补充 | 原因 |
|------|---------|------|
| 不知道 `q["key"]` vs `q.get("key")` | [] 不存在抛异常，.get() 返回None | 没接触过这个知识点 |

### 🔍 理解缺口
- `dict[key]` vs `dict.get(key)` — 还没学到.get()方法（Lv.2的内容）

### 🌟 亮点
- 自己加了 `while user_answer not in q['options']` 输入校验
- 用了 `if __name__ == '__main__':` 惯用法
- 百分比格式 `{correct / total * 100:.1f}%`
- `enumerate(questions, 1)` 从1开始计数

### 📌 核心教训
> **`dict[key]` 键不存在就崩，`dict.get(key)` 温柔返回None**

### Add
- 字典（dictionary），通过{}来创建字典(也可用dict()函数创建)。字典中的元素以键值对的形式存在，
例如：
```
person = {
    'name' : 'xiaoming',
    'age' : '18'
}
```
- 字典的方法：get()方法能用过对应的键返回对应的值，当键不存在于字典中时会返回None，
而使用索引运算如print(person['name'])则会引发KeyError异常。
字典的update()方法会将两个字典合并，x.update(y)会将x与y相同的键对应的值更新为y的值，
x中没有的键值对会按y中的添加进去。
- keys方法获取字典中所有的键，values方法获取字典中所有的值，而items方法会将键和值组成二元组
```
person = {'name': '王大锤', 'age': 25, 'height': 178}
print(person.keys())    # dict_keys(['name', 'age', 'height'])
print(person.values())  # dict_values(['王大锤', 25, 178])
print(person.items())   # dict_items([('name', '王大锤'), ('age', 25), ('height', 178)])
```
- pop和popitem方法能删除字典中的元素，前者会获得（返回）删除的元素，后者会获得（返回）键和值组成的二元组
del方法和列表一样也能删除元素，也许注意可能引发KeyError异常。clear方法会一键清空字典。
```
person = {'name': '王大锤', 'age': 25, 'height': 178, 'addr': '成都市武侯区科华北路62号1栋101'}
print(person.pop('age'))  # 25
print(person)             # {'name': '王大锤', 'height': 178, 'addr': '成都市武侯区科华北路62号1栋101'}
print(person.popitem())   # ('addr', '成都市武侯区科华北路62号1栋101')
print(person)             # {'name': '王大锤', 'height': 178}
person.clear()
print(person)             # {}
```
---

## 🗓️ 第7天（复盘日） <a id="day7"></a>

**主题**：Lv.1 总复盘

**得分**：87/100 🟢

---

### 一、名词解释（每题5分，共30分）
1. **`s = "abc"; s[1:3]` 的结果**     //作答：会报错，原因是索引越界 ❌ 不报错！`s[1:3]`取索引1,2 → `"bc"`。切片超出范围不会报错，`s[1:999]`也只会返回到末尾
2. **`list.append()` 和 `list.extend()` 的区别**     //作答：append将元素整体加进list，extend将元素拆开加。例如append([1,2])加整个列表，extend([1,2])加1,2 ✅
3. **`return` 和 `print` 的区别**     //未作答 🔴 return保留结果可继续使用，print只打印到屏幕不做保留
4. **`%` 和 `//` 的区别**     //作答：%是模运算，//是整除 ✅
5. **`==` 和 `=` 的区别**     //作答：==判断两个变量是否相等，=是赋值 ✅
6. **Falsy 值**     //作答：0，空列表，空字符串，False ✅

### 二、代码阅读 — 找bug（每题10分，共30分）
```python
# 问题1
def greet(name)           # ← 缺少冒号:  → SyntaxError
    print(f"你好，{name}！")
# 作答：一个错误，定义函数时没有加冒号: ✅
```
```python
# 问题2
nums = [3, 1, 2]
nums = nums.sort()
print(nums)
# 作答：输出None，因为sort()原地排序返回None ✅
```
```python
# 问题3
s = "Python"
for i in range(3):
    print(s[i])
# 作答：输出Pyt ✅  s[0]=P, s[1]=y, s[2]=t
```

### 三、手写代码（40分）
```python
def analyze_text(text):
    '''定义分析函数'''
    text_ana = {
        'length': len(text),
        'upper_text': text.upper(),    # 功能对，键名略偏离要求的"upper"
        'words': len(text.split()),    # ✅ 单词数
        'reversed': text[::-1]         # ✅ 反转
    }

    '''获取键和值'''
    for key, value in text_ana.items():
        print(f'{key} : {value}')

    return text_ana

if __name__ == "__main__":
    '''调用函数'''
    text = input()
    analyze_text(text)
```
🟢 功能完整，四项统计全部正确！额外加了遍历打印和`if __name__`

---

### ❌ 错误总结

| 错误 | 正确 | 原因 |
|------|------|------|
| `s[1:3]` 会报错 | `"bc"`，不报错 | 切片超范围不报错，D2就学过的！ |
| 漏了 return vs print | return保留结果，print只显示 | 忘了写 |

### 📌 核心教训
> **切片超范围不报错，自动截到末尾；D2学过的别忘！**

---

### 📊 Lv.1 最终成绩单

| 天数 | 主题 | 得分 |
|:----:|------|:----:|
| 🗓️ D1 | 变量/数据类型/print/input | 78 |
| 🗓️ D2 | 字符串操作/索引/切片 | 55 |
| 🗓️ D3 | 列表 + 循环 | 75 |
| 🗓️ D4 | if/逻辑运算符 | 76 |
| 🗓️ D5 | 函数 + return + 参数 | 78 |
| 🗓️ D6 | 综合项目CLI | 87 |
| 🗓️ D7 | Lv.1 总复盘 | 87 |
| **Lv.1 平均** | | **76.6 🟡** |

---

### 🔑 Lv.1 遗留薄弱点（Lv.2重点补强）

1. 🔴 **切片边界规则** — D2和D7都在这栽跟头
2. 🟡 **字符串加引号** — D5 return yes 没加引号
3. 🟡 **`%`和`//`区分** — D4混淆过

---

> 📌 Lv.1 结束，Lv.2+ 内容见各周独立文档
