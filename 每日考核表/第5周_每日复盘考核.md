# 📝 Python 45天每日复盘考核 — 第5周

> 学习范围：Lv.5 — 独立项目（第29~35天）
> 在 VSCode 中 **Ctrl + 点击** 下方链接可快速跳转。

---

## 📑 目录（Ctrl+点击跳转）

| <a href="#day29">第29天</a> | <a href="#day30">第30天</a> | <a href="#day31">第31天</a> | <a href="#day32">第32天</a> | <a href="#day33">第33天</a> | <a href="#day34">第34天</a> | <a href="#day35">第35天(复盘)</a> |
|---|---|---|---|---|---|---|

---

## 🗓️ 第29天 <a id="day29"></a>

**主题**：项目结构 + 模块化设计

**学习内容**：
```
标准 Python 项目结构：
my_project/
├── README.md                 # 项目说明
├── requirements.txt          # 依赖清单
├── .gitignore                # git 忽略文件
├── my_project/               # 主包
│   ├── __init__.py           # 文件夹变"包"的标记
│   ├── main.py               # 程序入口
│   ├── config.py             # 配置
│   ├── models.py             # 数据模型
│   ├── utils.py              # 工具函数
│   └── services/             # 子模块
│       ├── __init__.py
│       └── user_service.py
└── tests/                    # 测试
    └── test_main.py
```

```python
# __init__.py — 文件夹变身"包"
from my_project import models, utils
from my_project.services import user_service

# if __name__ == "__main__" — 双重身份
def add(a, b):
    return a + b
if __name__ == "__main__":
    print("直接运行时执行")     # import 时这里不执行

# 绝对导入 vs 相对导入
from my_project import models              # 绝对（完整包名）
from .models import User                   # 相对（同包用.）

# requirements.txt 版本管理
requests==2.31.0       # == 精确版本
pandas>=2.0.0          # >= 最低版本
pytest~=8.0            # ~= 兼容8.x
```

**口诀**：__init__.py是门牌，文件夹变包；if __name__双身份，直接跑就执行，import不执行；绝对导入用全名，相对导入用点；main.py只指挥，功能分文件放；requirements锁版本

**得分**：96/100 🟢

---

### 一、名词解释（每题5分，共20分）
1. **`__init__.py` 作用**     //作答：标识该目录为包，方便后续导入 ✅
2. **`if __name__` 解决问题**     //作答：防止目标文件作为导入文件时运行其代码 ✅
3. **绝对 vs 相对导入**     //作答：绝对导入需完整包名，包名修改会异常；相对导入简洁，包名修改不影响 ✅
4. **`==` vs `~=`**     //作答：==具体版本，~=兼容版本如~=8.0兼容8.x ✅

### 二、代码阅读（每题10分，共20分）
```python
# 问题1 — utils.py直接跑 → __main__块 → "你好，直接运行"；main.py导入 → 不执行块
# 作答：utils输出"你好，直接运行"，main输出"你好，导入使用" ✅
```
```python
# 问题2 — 从包外导入user模块
# 作答：from my_project.services.user import get_user ✅
```

### 三、代码补全（10分）
```python
if __name__ == "__main__":      # ✅
```

### 四、手写代码（30分）
**题目**：重构计算器 — operations.py + main.py + __init__.py
```python
# operations.py
def add(a, b): return a + b
def subtract(a, b): return a - b
def multiply(a, b): return a * b
def divide(a, b):
    if b == 0:
        raise ValueError        # ✅ 除零处理
    return a / b

# main.py
from operations import *        # ⚠️ 通配符导入不推荐
def get_nums():
    while True:
        try:
            parts = input("请输入").split()
            if len(parts) != 3:
                print("请输入三部分：数字，运算符，数字")
                continue
            a, op, b = parts
            return float(a), op, float(b)
        except ValueError:
            print("无效输入，请重新输入")

if __name__ == "__main__":
    a, op, b = get_nums()
    if op == '-': result = subtract(a, b)
    elif op == '+': result = add(a, b)
    elif op == '*': result = multiply(a, b)
    elif op == '/': result = divide(a, b)
    else: print("请输入正确的运算符")
    print(result)
```
✅ 输入验证+异常处理+分派逻辑完整 🟡 通配符导入应改为明确列出

### 五、概念辨析（20分）
| 概念A | 概念B | 作答与点评 |
|-------|-------|-----------|
| 包（package） | 模块（module） | //作答：包是带__init__.py的目录，模块是.py文件，包是模块容器 ✅ |
| `from .config import x` | `from my_project.config import x` | //作答：前者相对导入包名改变不影响，后者绝对导入会异常 ✅ |

---

### 📌 核心教训
> **通配符 `import *` 会污染命名空间，明确列出更清晰**

---

## 🗓️ 第30天 <a id="day30"></a>

**主题**：设计模式 — 单例 + 工厂 + 装饰器

**学习内容**：
```python
# 单例模式 — 全程序只有一个实例
class Logger:
    _instance = None
    def __new__(cls, *args, **kwargs):
        # __new__ 在 __init__ 之前，负责创建对象
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance        # 每次返回同一个
    def log(self, msg):
        print(f"[LOG] {msg}")

l1 = Logger(); l2 = Logger()
print(l1 is l2)     # True

# 工厂模式 — 按需生产对象
class Circle:
    def draw(self): return "绘制圆形"
class Square:
    def draw(self): return "绘制方形"

class ShapeFactory:
    @staticmethod
    def create(shape_type):
        shapes = {"circle": Circle, "square": Square}
        cls = shapes.get(shape_type)
        if cls is None:
            raise ValueError(f"未知形状：{shape_type}")
        return cls()

shape = ShapeFactory.create("circle")
print(shape.draw())   # 绘制圆形

# 装饰器模式 — 给对象加功能（不用继承）
class Coffee:
    def cost(self): return 10
    def desc(self): return "咖啡"

class CoffeeDecorator:
    def __init__(self, coffee):
        self._coffee = coffee
    def cost(self): return self._coffee.cost()
    def desc(self): return self._coffee.desc()

class Milk(CoffeeDecorator):
    def cost(self): return self._coffee.cost() + 2
    def desc(self): return self._coffee.desc() + "，加牛奶"

class Sugar(CoffeeDecorator):
    def cost(self): return self._coffee.cost() + 1
    def desc(self): return self._coffee.desc() + "，加糖"

coffee = Sugar(Milk(Coffee()))
print(coffee.desc())   # 咖啡，加牛奶，加糖
print(coffee.cost())   # 13
```

**三模式对比**：
| 模式 | 解决什么问题 | 一句话 |
|------|------------|--------|
| 单例 | 全局只需一个对象 | 全程序只有一个 |
| 工厂 | 创建对象逻辑集中 | 按需生产 |
| 装饰器 | 动态加功能不修改原类 | 层层贴贴纸 |

**口诀**：单例模式全局一，__new__里拦创建；工厂模式集中管，按名造对象；装饰器模式贴贴纸，功能层层加

**得分**：99/100 🟢🏆

---

### 一、名词解释（每题5分，共20分）
1. **单例模式目的**     //作答：确保全局只共用这一个实例，关键方法是__new__ ✅
2. **工厂模式解决什么**     //作答：解决需要反复创建实例的问题，将创建实例封装起来 ✅
3. **装饰器模式 vs 继承**     //作答：装饰器简便对一方法创建多类、解耦合；继承多方法会类爆炸 ✅
4. **`__new__` vs `__init__`**     //作答：前者创建新对象交给init初始化，后者创建类时初始化 🟡 __init__是对象创建后初始化，不是"创建类时"

### 二、代码阅读（每题10分，共20分）
```python
# 问题1 — 单例
print(s1 is s2)  # 作答：True ✅
```
```python
# 问题2 — 工厂创建Dog/Cat
print(a1.speak(), a2.speak())  # 作答：汪汪 喵喵 ✅
```

### 三、代码补全（10分）
```python
if cls._instance is None:                    # ✅
    cls._instance = super().__new__(cls)     # ✅
return cls._instance                         # ✅
```

### 四、手写代码（30分）
**题目**：工厂模式实现通知系统 Email/SMS/Push + NotifierFactory
```python
class EmailNotifier:
    def send(self, msg): print(f'email:{msg}')
class SMSNotifier:
    def send(self, msg): print(f'SMS: {msg}')
class PushNotifier:
    def send(self, msg): print(f'Push :{msg}')

class NotifierFactory:
    _PushType = {"email": EmailNotifier, "sms": SMSNotifier, "push": PushNotifier}
    @classmethod                    # 👏 自己用classmethod！
    def create(cls, notify_type):
        push_type = cls._PushType.get(notify_type)
        if push_type is None:
            raise ValueError(f'未知通知类型:{notify_type}')
        return push_type()
```
✅ 三通知类+工厂+ValueError全对 👏 @classmethod 用得很好 30/30

### 五、概念辨析（20分）
| 概念A | 概念B | 作答与点评 |
|-------|-------|-----------|
| 单例 `is` 相同 | 普通类每次 new | //作答：前者实例都相同，后者都不相同 ✅ |
| 工厂模式 | 直接 `Dog()` | //作答：工厂整合创建，加新只需改字典；直接实例化多次创建麻烦 ✅ |

---

### 📌 核心教训
> **__init__ 是对象创建后初始化，不是"创建类时"**

---

## 🗓️ 第31天 <a id="day31"></a>

*待学习与考核……*

---

## 🗓️ 第32天 <a id="day32"></a>

*待学习与考核……*

---

## 🗓️ 第33天 <a id="day33"></a>

*待学习与考核……*

---

## 🗓️ 第34天 <a id="day34"></a>

*待学习与考核……*

---

## 🗓️ 第35天（复盘日） <a id="day35"></a>

*待学习与考核……*
